# Optional shared secret to gate POST /render
RENDER_API_KEY=

# Public base URL for brand assets (logos) and where rendered images are served from
ASSET_BASE=https://assets.drivertrack.co

# Cloudflare R2 (S3-compatible) for uploading rendered images
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_BASE=https://assets.drivertrack.co
